<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>KB 손해보험 김포 한강 지점 이미지 서비스</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body {{#success}}data-success="{{success}}" data-image-url="{{imageUrl}}" data-s3-key="{{s3Key}}" {{/success}}data-message="{{message}}" {{#isQuotaExceeded}}data-is-quota-exceeded="{{isQuotaExceeded}}"{{/isQuotaExceeded}}>
    <div class="container">
        <!-- 헤더 -->
        <div class="header">
            <!-- 좌측: 전체 보기 & 즐겨찾기 버튼 -->
            <div class="header-left">
                <button id="viewAllBtn" onclick="window.location.href='/list'" class="btn-view-all-left">
                    📚 전체보기
                </button>
                <button type="button" id="favoritesBtn" onclick="goToFavorites()" class="btn-favorites-left">
                    ❤️ 즐겨찾기
                </button>
            </div>

            <!-- 중앙: 제목 -->
            <div class="header-content">
                <h1>KB 손해보험 김포 한강 지점 이미지 서비스</h1>
                <p>{{message}}</p>
            </div>

            <!-- 우측: 사용자 정보 & 로그인 -->
            <div class="header-top">
                {{#User}}
                <div class="user-info">
                    <span class="user-email">{{User.email}}</span>
                    <a href="/auth/logout" class="btn-logout">로그아웃</a>
                </div>
                {{/User}}
                {{^User}}
                <a href="/oauth2/authorization/google" class="btn-login-google">
                    <svg class="google-icon" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
                        <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
                        <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/>
                        <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
                    </svg>
                    Google 로그인
                </a>
                {{/User}}
            </div>
        </div>

        <!-- 메인 콘텐츠 -->
        <div class="content">
            <!-- 로딩 표시 -->
            <div class="loading" id="loadingDiv">
                <div class="spinner"></div>
                <p>이미지를 생성 중입니다. 잠깐만 기다려주세요...</p>
            </div>
            <div id="resultDiv" style="display: none;">
                <div class="image-container">
                    <img id="resultImage" alt="생성된 이미지" data-s3-key="" onclick="openImageInNewTab()" style="max-width: 100%; height: auto; border-radius: 8px; box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1); margin-bottom: 20px; cursor: pointer;" title="클릭하면 새 창에서 열립니다" />
                    <div class="image-actions">
                        <a id="downloadBtn" download class="btn-download" target="_blank">
                            ⬇️ 다운로드
                        </a>
                        <button type="button" class="btn-save-favorite" onclick="saveFavorite()">
                            ❤️ 즐겨찾기에 저장
                        </button>
                    </div>
                </div>
            </div>
            <!-- 이미지 생성 폼 -->
            <form id="generateForm" action="/generate" method="get" onsubmit="showLoading(event)" style="width: 100%;">
                <!-- 이미지 첨부 -->
                <div class="form-group">
                    <label for="attachImage">
                        🖼️ 이미지 첨부 (선택사항)
                    </label>
                    <div class="image-attachment-container">
                        <input type="file" id="attachImage" name="attachImage" accept="image/*" style="display: none;" onchange="handleImageAttachment(event)">
                        <button type="button" class="btn-attach-image" onclick="document.getElementById('attachImage').click()">
                            📎 이미지 첨부
                        </button>
                        <div id="attachmentStatus" class="attachment-status" style="display: none;">
                            <span id="attachmentFileName"></span>
                            <button type="button" class="btn-remove-attachment" onclick="removeAttachment()">✕</button>
                        </div>
                    </div>
                    <small style="color: #888; margin-top: 8px; display: block;">
                        💡 첨부된 이미지를 기반으로 이미지를 생성합니다.
                    </small>
                </div>

                <!-- 프롬프트 입력 -->
                <div class="form-group">
                    <label for="prompt">
                        🎯 이미지 생성 조건 <span style="color: red;">*</span>
                    </label>
                    <textarea
                        id="prompt"
                        name="prompt"
                        placeholder="힌트: 이미지에 필요한 속성(사이즈 등)과 필요한 내용을 구체적으로 작성해주세요."
                        required>{{#prompt}}{{prompt}}{{/prompt}}</textarea>
                </div>


                <!-- 버튼 그룹 -->
                <div class="button-group">
                    <button type="button" class="btn-generate" onclick="document.getElementById('generateForm').dispatchEvent(new Event('submit'))">
                        ✨ 이미지 생성
                    </button>
                    <button type="button" class="btn-reset" onclick="confirmReset()">
                        🔄 초기화
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="/js/script.js"></script>
</body>
</html>